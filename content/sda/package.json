{
  "name": "@sda/content",
  "version": "0.0.0",
  "license": "MIT",
  "private": true,
  "type": "module",
  "description": "The SDA content pack: the seed property registry and component manifests (pure DATA referencing the shared vocabulary by id). All system-design meaning lives here, never in the engine.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@sda/engine-core": "workspace:*",
    "@sda/engine-sim": "workspace:*",
    "@sda/engine-solve": "workspace:*",
    "@sda/solver-contract": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "clingo-wasm": "^0.3.2",
    "fast-check": "^3.23.1",
    "typescript": "^5.6.3"
  }
}
