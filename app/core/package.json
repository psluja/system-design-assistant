{
  "name": "@sda/core",
  "version": "0.0.0",
  "license": "MIT",
  "private": true,
  "type": "module",
  "description": "The controllable command core (doc-9): the project Document, typed commands with Results + history, serialization, and engine wiring. Headless; the single source of truth every client (MCP, UI, WebMCP) drives.",
  "exports": { ".": "./src/index.ts" },
  "scripts": { "typecheck": "tsc --noEmit" },
  "dependencies": {
    "@sda/content": "workspace:*",
    "@sda/engine-core": "workspace:*",
    "@sda/engine-solve": "workspace:*"
  },
  "devDependencies": { "fast-check": "^4.9.0", "typescript": "^5.6.3" }
}
