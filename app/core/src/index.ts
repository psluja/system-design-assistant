// @sda/core — the controllable command core. Headless single source of truth that the MCP
// server, the web UI, and WebMCP all drive through the same commands.
export * from './document';
export * from './commands';
export { Studio } from './store';
