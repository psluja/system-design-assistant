{
  "name": "@sda/presenter",
  "version": "0.0.0",
  "license": "MIT",
  "private": true,
  "type": "module",
  "description": "Shared VIEW-MODELS for every SDA shell (web, VS Code): problems rows, status line, summary sections, node detail, layout, suggestions. Both shells render these verbatim — one presenter, zero drift.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@sda/content": "workspace:*",
    "@sda/core": "workspace:*",
    "@sda/engine-core": "workspace:*",
    "@sda/engine-solve": "workspace:*"
  },
  "devDependencies": {
    "@types/dagre": "^0.7.54",
    "dagre": "0.8.5",
    "elkjs": "0.11.1",
    "typescript": "^7.0.2",
    "vitest": "^2.1.4"
  }
}
